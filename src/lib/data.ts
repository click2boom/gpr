import type { Project,Proxy,Context } from "./type"
const InitProject:Project = {
    author:'zen-browser',name:'desktop',file:'zen.installer.exe',version:'latest'
}

const InitProxy:Proxy = {
    url:'',name:'直连'
}

export {InitProject,InitProxy}